# 交付说明：quoting-ui2code-sync

## 依据
- `UI2Code (11).zip` / `CHANGELOG.md`
- 模式：P2C（字段同步，样式沿用现有 LUI）

## 变更
1. **统计对象**：计费策略为「统计考核 / 统计+合单」时，在报价明细工具栏展示（产品报价与场景报价均支持）。
2. **统计累进方式**：第3步报价明细中已移除该字段。
3. **样式**：未改 `global.css` 与其他页面，沿用现有 `detail-control` / `lui-control`。

## 范围
仅 `src/views/OnestopQuoting.vue`。

## 验证建议
1. 一站式报价 → 场景报价 → 计费策略选「统计考核」或「统计+合单」→ 进入报价明细，应见「统计对象」，不见「统计累进方式」。
2. 产品报价同样策略下：保留「统计对象」，无「统计累进方式」；统计上下限列仍展示。
3. 策略为「不统计不合单」时：无统计对象、无统计上下限列。
